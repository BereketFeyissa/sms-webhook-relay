# Example environment variables for sms-webhook-relay

# Kanel gateway URL (full endpoint)
KANEL_URL=https://kanel.example/cgi-bin/sendsms

# Kanel credentials
KANEL_USER=your_username
KANEL_PASS=your_password

# Sender ID shown as the "from" for SMS
KANEL_SENDER=RelaySender

# Default recipient phone number (used when alert doesn't include `phone` label)
SMS_TO=+2519XXXXXXXX

# Optional: if set, incoming requests must include header `x-webhook-token` with this value
WEBHOOK_SECRET=super-secret-token
